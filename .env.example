# =============================================================================
# ENVIRONMENT VARIABLES FOR MCP SERVERS
# =============================================================================
# Copy this file to .env and fill in your actual values
# IMPORTANT: .env is gitignored - never commit secrets to version control
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED - Core Services
# -----------------------------------------------------------------------------

# GitHub API (Required for github MCP server)
# Create at: https://github.com/settings/tokens
# Scopes needed: repo, read:org, workflow
GITHUB_TOKEN=ghp_your_github_personal_access_token_here

# -----------------------------------------------------------------------------
# OPTIONAL - DevOps Infrastructure
# -----------------------------------------------------------------------------

# Terraform Cloud (Optional - for Terraform MCP server)
# Create at: https://app.terraform.io/app/settings/tokens
# Only needed if using HCP Terraform
TERRAFORM_TOKEN=your_terraform_cloud_api_token_here
TFE_ADDRESS=https://app.terraform.io

# Kubernetes (Optional - for Kubernetes MCP server)
# Uses your existing kubeconfig by default
# Only set if using custom config location
KUBECONFIG=${HOME}/.kube/config

# Prometheus (Optional - for Prometheus MCP server)
# URL to your Prometheus instance
PROMETHEUS_URL=http://localhost:9090

# -----------------------------------------------------------------------------
# OPTIONAL - Security & Quality
# -----------------------------------------------------------------------------

# Semgrep (Optional - for Semgrep MCP server)
# Create at: https://semgrep.dev/orgs/-/settings/tokens
# Enables SAST security scanning
SEMGREP_APP_TOKEN=your_semgrep_app_token_here

# -----------------------------------------------------------------------------
# OPTIONAL - Data & Storage
# -----------------------------------------------------------------------------

# PostgreSQL (Optional - for PostgreSQL MCP server)
# Format: postgresql://user:password@host:port/database
POSTGRES_URL=postgresql://user:password@localhost:5432/mydb

# -----------------------------------------------------------------------------
# OPTIONAL - External Services
# -----------------------------------------------------------------------------

# Brave Search API (Optional - for brave-search MCP server)
# Create at: https://api.search.brave.com/register
BRAVE_API_KEY=your_brave_search_api_key_here

# Slack (Optional - for Slack MCP server)
# Create at: https://api.slack.com/apps
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token-here
SLACK_TEAM_ID=T0123456789

# -----------------------------------------------------------------------------
# OPTIONAL - Context & Memory
# -----------------------------------------------------------------------------

# Context Server Storage (Optional - customizes storage location)
CONTEXT_STORAGE_PATH=.metaHub/orchestration/context/
MAX_CONTEXT_MB=50
RETENTION_HOURS=168

# -----------------------------------------------------------------------------
# OPTIONAL - Testing & Automation
# -----------------------------------------------------------------------------

# Playwright Browser Settings (Optional)
PLAYWRIGHT_HEADLESS=true
PLAYWRIGHT_TIMEOUT=60000
PLAYWRIGHT_BROWSERS_PATH=.metaHub/playwright-browsers

# Puppeteer Settings (Optional)
PUPPETEER_HEADLESS=true
PUPPETEER_TIMEOUT=30000

# -----------------------------------------------------------------------------
# ANTHROPIC API (For MCP CLI)
# -----------------------------------------------------------------------------

# Anthropic API Key (for MCP CLI provider)
# Create at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-api03-your-anthropic-api-key-here

# -----------------------------------------------------------------------------
# OPTIONAL - Alternative LLM Providers
# -----------------------------------------------------------------------------

# OpenAI API Key (Optional - alternative provider)
OPENAI_API_KEY=sk-your-openai-api-key-here

# -----------------------------------------------------------------------------
# NOTES
# -----------------------------------------------------------------------------
#
# 1. Required Variables:
#    - GITHUB_TOKEN: Needed for github MCP server (core functionality)
#
# 2. Priority-1 Optional Variables:
#    - SEMGREP_APP_TOKEN: For security scanning (highly recommended)
#    - TERRAFORM_TOKEN: For infrastructure automation
#    - ANTHROPIC_API_KEY: For MCP CLI usage
#
# 3. Priority-2 Optional Variables:
#    - PROMETHEUS_URL: For monitoring
#    - BRAVE_API_KEY: For web search
#    - POSTGRES_URL: For database operations
#
# 4. Environment Variable Substitution:
#    - Use ${VAR_NAME} syntax in .ai/mcp/mcp-servers.json
#    - Example: "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
#
# 5. Security Best Practices:
#    - Never commit .env to version control
#    - Rotate tokens regularly
#    - Use minimum required permissions for each token
#    - Consider using secret management tools for production
#
# 6. Testing:
#    - Test with dry-run mode first:
#      python .metaHub/scripts/devops_workflow_runner.py --dry-run
#
# =============================================================================
