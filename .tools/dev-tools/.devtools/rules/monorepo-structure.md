# Monorepo Structure Rules

- All new apps go in `apps/` directory
- All shared packages go in `packages/` directory
- Use pnpm workspaces for dependency management
- Follow existing automation patterns in `.automation/`
- Never modify existing `organizations/` or `metaHub/` without explicit permission
- Respect the existing monorepo structure and conventions
