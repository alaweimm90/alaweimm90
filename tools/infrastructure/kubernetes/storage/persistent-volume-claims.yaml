---
# SuperTool Data PVC (Production)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: supertool-data
  namespace: production
  labels:
    app.kubernetes.io/name: supertool
    app.kubernetes.io/instance: supertool-production
    app.kubernetes.io/component: storage
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: fast-ssd
  resources:
    requests:
      storage: 50Gi
  volumeMode: Filesystem

---
# SuperTool Cache PVC (Production)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: supertool-cache
  namespace: production
  labels:
    app.kubernetes.io/name: supertool
    app.kubernetes.io/instance: supertool-production
    app.kubernetes.io/component: cache
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: fast-ssd
  resources:
    requests:
      storage: 20Gi
  volumeMode: Filesystem

---
# SuperTool Logs PVC (Production)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: supertool-logs
  namespace: production
  labels:
    app.kubernetes.io/name: supertool
    app.kubernetes.io/instance: supertool-production
    app.kubernetes.io/component: logging
spec:
  accessModes:
    - ReadWriteMany # Multiple pods can write logs
  storageClassName: nfs-storage
  resources:
    requests:
      storage: 100Gi
  volumeMode: Filesystem

---
# SuperTool Backup PVC (Production)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: supertool-backup
  namespace: production
  labels:
    app.kubernetes.io/name: supertool
    app.kubernetes.io/instance: supertool-production
    app.kubernetes.io/component: backup
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard-hdd
  resources:
    requests:
      storage: 200Gi
  volumeMode: Filesystem

---
# SuperTool Shared Config PVC (Production)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: supertool-shared-config
  namespace: production
  labels:
    app.kubernetes.io/name: supertool
    app.kubernetes.io/instance: supertool-production
    app.kubernetes.io/component: config
spec:
  accessModes:
    - ReadOnlyMany # Shared read-only config
  storageClassName: nfs-storage
  resources:
    requests:
      storage: 5Gi
  volumeMode: Filesystem

---
# SuperTool Data PVC (Staging)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: supertool-data
  namespace: staging
  labels:
    app.kubernetes.io/name: supertool
    app.kubernetes.io/instance: supertool-staging
    app.kubernetes.io/component: storage
    environment: staging
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard-hdd
  resources:
    requests:
      storage: 20Gi
  volumeMode: Filesystem

---
# SuperTool Data PVC (Dev)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: supertool-data
  namespace: dev
  labels:
    app.kubernetes.io/name: supertool
    app.kubernetes.io/instance: supertool-dev
    app.kubernetes.io/component: storage
    environment: development
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard-hdd
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
